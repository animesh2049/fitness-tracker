package com.animesh.workouttracker.di

import android.content.Context

/**
 * Manual dependency container. Repositories and engines are added here as the
 * data and domain layers land (Milestones 1 and 2).
 */
class AppContainer(val appContext: Context)
